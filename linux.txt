
ps -aux

du -hd 1

lscpu

sed -i.bak 's/\r$//g' <file>

find . -type f -exec ls -l {} \;
find /tmp -ctime +10 -exec rm -rf {} +

sudo apt list --installed | grep uuid

sudo systemctl start|stop|restart  NetworkManager

ss -tunlp | grep <port>
nslookup <server>

ifconfig | grep inet | grep -v inet6 | awk '{print $2}' | head -1

echo -n tmuser | base64

nm -D --defined-only /lib/libtest.so

last
last reboot

get pid
cat /proc/<pid>/cgroup

rpm -ql <package>
rpm -qa | grep openjdk | xargs  yum -y remove

yum group list
yum groupinstall "Server with GUI" | "Development Tools"

yum-config-manager --enablerepo=<repo>
yum clean all && yum makecache && yum update


# selinux
sudo setenforce 0
getenforce
/etc/selinux/config

# check/set password never expires
chage -l <username>
sudo chage -M 1 <username>

# BIOS check
sudo dmidecode -s bios-version

# Screenshot
export DIPSLAY
import -window root image.png
