
# selinux
sudo setenforce 0
getenforce
/etc/selinux/config

---
# Stop SELinux complaining about PVs

sudo semanage fcontext -a -t container_file_t '/mnt/data/containers(/.*)?'

sudo restorecon -Rv /mnt/data/containers

ls -Zd /mnt/data/containers/mysql8-mysql-8.4.0
>That's exactly what you want to see.
>system_u:object_r:container_file_t:s0

---
